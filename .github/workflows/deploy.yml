name: Deploy

on:
  push:
    branches: [main]

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy-backend:
    name: Deploy Backend to Render
    runs-on: ubuntu-latest
    if: secrets.RENDER_DEPLOY_HOOK_URL != ''
    steps:
      - name: Trigger Render Deploy Hook
        run: |
          curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"

  deploy-frontend:
    name: Deploy Frontend to Vercel
    runs-on: ubuntu-latest
    if: secrets.VERCEL_TOKEN != ''
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Build frontend
        working-directory: frontend
        run: npm run build
        env:
          VITE_API_URL: ${{ vars.VITE_API_URL }}
          VITE_STRIPE_PUBLIC_KEY: ${{ vars.VITE_STRIPE_PUBLIC_KEY }}
          VITE_APP_URL: ${{ vars.VITE_APP_URL }}
          VITE_SENTRY_DSN: ${{ vars.VITE_SENTRY_DSN }}

      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-args: --prod
          working-directory: frontend

  deploy-skip-warning:
    name: Deploy skipped (configure secrets)
    runs-on: ubuntu-latest
    if: secrets.RENDER_DEPLOY_HOOK_URL == '' && secrets.VERCEL_TOKEN == ''
    steps:
      - name: Print setup instructions
        run: |
          echo "::warning::Deploy jobs skipped. Configure GitHub secrets to enable auto-deploy:"
          echo "::warning::  - RENDER_DEPLOY_HOOK_URL (for backend)"
          echo "::warning::  - VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID (for frontend via Actions)"
          echo "::warning::Note: Vercel auto-deploys via its own GitHub integration are not affected."
